import {CreateLayout} from '@/layouts';
import {UpdateProfileForm} from '@/components';

const ProfilePage = () => {
  return (
    <CreateLayout>
      <UpdateProfileForm />
    </CreateLayout>
  );
};

export default ProfilePage;
